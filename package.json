{
  "name": "dairy_app_",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server &&(cd server && npm install) ||(cd client && npm install)",
    "client": "npm start --prefix client",
    "server": "npm run backend --prefix server",
    "all": "npm run server & npm run client"
  },
  "keywords": [],
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
