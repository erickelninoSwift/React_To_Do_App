{
  "name": "dairy_app_",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server &&(cd server && npm install) ||(cd client && npm install)",
    "client": "npm start --prefix client || cd client && npm start",
    "server": "npm run backend --prefix server || cd server && npm run backend"
  },
  "keywords": [],
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
